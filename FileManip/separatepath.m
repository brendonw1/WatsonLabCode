function [higherpath,remainder]=separatepath(path)%given an input path, this gives the path for a level upfor c=1:length(path);%for creating a new directory at the same level as the one selected, but with a different name    u(c)=strcmp('\',path(c));%find where slashes in the directory pathendu=find(u);u=u(end);%take the coordinate of the lasthigherpath=path(1:u-1);%save up to that... to get path up to that pointremainder=path(u+1:end);